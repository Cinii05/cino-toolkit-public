interface:
  display_name: TMC UI Master
  short_description: Audit, implement and verify every TMC UI issue
  default_prompt: Inspect the current TMC frontend, fix the authorised UI issues,
    and verify the rendered result.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
